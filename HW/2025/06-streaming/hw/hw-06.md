# Homework

В этом домашнем задании мы изучим потоковую передачу с помощью PyFlink.
Вместо Kafka мы будем использовать Red Panda, которая является заменой Kafka.
Она реализует тот же интерфейс, поэтому мы можем использовать библиотеку Kafka
для Python для связи с ней, а также использовать коннектор Kafka в PyFlink.

Для этого домашнего задания мы будем использовать данные Taxi:
- Green 2019-10 data from [here](https://github.com/DataTalksClub/nyc-tlc-data/releases/download/green/green_tripdata_2019-10.csv.gz)


## Setup

We need:

- Red Panda
- Flink Job Manager
- Flink Task Manager
- Postgres

It's the same setup as in the [pyflink module](../../../06-streaming/pyflink/), so go there and start docker-compose:

```bash
cd ../../../06-streaming/pyflink/
docker-compose up
```

(Add `-d` if you want to run in detached mode)

Visit http://localhost:8081 to see the Flink Job Manager

Connect to Postgres with pgcli, pg-admin, [DBeaver](https://dbeaver.io/) or any other tool.

The connection credentials are:

- Username `postgres`
- Password `postgres`
- Database `postgres`
- Host `localhost`
- Port `5432`

With pgcli, you'll need to run this to connect:

```bash
pgcli -h localhost -p 5432 -u postgres -d postgres
```

Запустите эти запросы, чтобы создать целевую зону Postgres для первых событий и окон:

```sql 
CREATE TABLE processed_events (
    test_data INTEGER,
    event_timestamp TIMESTAMP
);

CREATE TABLE processed_events_aggregated (
    event_hour TIMESTAMP,
    test_data INTEGER,
    num_hits INTEGER 
);
```

## Вопрос 1: Версия Redpanda

Now let's find out the version of redpandas. 

For that, check the output of the command `rpk help` _inside the container_. The name of the container is `redpanda-1`.

Find out what you need to execute based on the `help` output.

Какая версия, судя по выводу выполненной вами команды? (скопируйте полную версию)


## Question 2. Creating a topic

Прежде чем отправлять данные на сервер redpanda, нам нужно создать тему. 
Мы делаем это также с помощью `rpk` команды, которую мы использовали ранее для выяснения версии redpandas.

Прочитайте вывод `help` и на его основе создайте тему с названием `green-trips`

Какой вывод команды создания темы? Включите весь вывод в свой ответ.

## Question 3. Connecting to the Kafka server

Нам нужно убедиться, что мы можем подключиться к серверу, чтобы позже мы могли отправить некоторые данные в его темы.

Сначала давайте установим коннектор Kafka (хотите ли вы иметь для этого отдельную виртуальную среду — решать вам)

```bash
pip install kafka-python
```

You can start a jupyter notebook in your solution folder or
create a script

Let's try to connect to our server:

```python
import json

from kafka import KafkaProducer

def json_serializer(data):
    return json.dumps(data).encode('utf-8')

server = 'localhost:9092'

producer = KafkaProducer(
    bootstrap_servers=[server],
    value_serializer=json_serializer
)

producer.bootstrap_connected()
```

При условии, что вы можете подключиться к серверу, каков вывод последней команды?

## Question 4: Sending the Trip Data

Now we need to send the data to the `green-trips` topic

Read the data, and keep only these columns:

* `'lpep_pickup_datetime',`
* `'lpep_dropoff_datetime',`
* `'PULocationID',`
* `'DOLocationID',`
* `'passenger_count',`
* `'trip_distance',`
* `'tip_amount'`

Now send all the data using this code:

```python
producer.send(topic_name, value=message)
```

For each row (`message`) in the dataset. In this case, `message`
is a dictionary.

После отправки всех сообщений очистите данные:

```python
producer.flush()
```

Use `from time import time` to see the total time 

```python
from time import time

t0 = time()

# ... your code

t1 = time()
took = t1 - t0
```

Сколько времени заняла отправка всего набора данных и очистка?


## Вопрос 5: Построение окна сеансизации (2 балла)

Теперь у нас есть данные в потоке Kafka. Пришло время их обработать.

* Copy `aggregation_job.py` and rename it to `session_job.py`
* Have it read from `green-trips` fixing the schema
* Используйте [session window](https://nightlies.apache.org/flink/flink-docs-master/docs/dev/datastream/operators/windows/) с интервалом в 5 минут.
* Используйте `lpep_dropoff_datetime` время в качестве водяного знака с погрешностью в 5 секунд.
* В каких пунктах посадки и высадки пассажиров самая длинная непрерывная серия поездок на такси?


## Submitting the solutions

- Form for submitting: https://courses.datatalks.club/de-zoomcamp-2025/homework/hw6
- Deadline: See the website

